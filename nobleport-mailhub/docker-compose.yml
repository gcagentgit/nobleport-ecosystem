services:
  mailhub:
    build: .
    env_file: .env
    environment:
      MAILHUB_HOST: 0.0.0.0
    ports:
      - "127.0.0.1:8025:8025"
    volumes:
      - mailhub-data:/data
    restart: unless-stopped

volumes:
  mailhub-data:
